export class ScreenShare {
  static async share(ip) {
    console.log(ip);

    return new Promise(() => {});
  }

  static async view(ip) {
    console.log(ip);

    return new Promise(() => {});
  }
}
